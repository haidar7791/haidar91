// src/MapConfig.js

// حجم كل خانة تايل tile (مثلاً 64 بكسل)
export const TILE_SIZE = 50;

// 🛑🛑🛑 الأرقام القابلة للتعديل 🛑🛑🛑
// عدد الخانات أفقياً (العرض): قم بزيادة هذا الرقم لتكبير الخريطة أفقياً
export const MAP_TILES_X = 16; 
// عدد الخانات عمودياً (الارتفاع): قم بزيادة هذا الرقم لتكبير الخريطة عمودياً
export const MAP_TILES_Y = 16;

// 🛑🛑🛑 الحجم الفعلي للخريطة بالبكسل (يُحسب تلقائياً) 🛑🛑🛑
export const MAP_WIDTH = MAP_TILES_X * TILE_SIZE;
export const MAP_HEIGHT = MAP_TILES_Y * TILE_SIZE;

// يمكن إضافة إعدادات إضافية هنا
export default {
    TILE_SIZE,
    MAP_TILES_X,
    MAP_TILES_Y,
    MAP_WIDTH,
    MAP_HEIGHT,
};
